import requests

# requests module to call APIs already defined in app.py and displays the returned value

url = 'http://localhost:5000/predict_api'
r = requests.post(url,json={'experience':2, 'test_score':9, 'interview_score':6})

print(r.json())